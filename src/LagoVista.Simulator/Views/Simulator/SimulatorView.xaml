<?xml version="1.0" encoding="utf-8" ?>
<pge:LagoVistaContentPage
      xmlns:pge="clr-namespace:LagoVista.XPlat.Core;assembly=LagoVista.XPlat.Core"
      xmlns="http://xamarin.com/schemas/2014/forms"
      xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
      x:Class="LagoVista.Simulator.Views.Simulator.SimulatorView"
      RightMenu="Edit"
      Title="Simulator Messages">
  
  
  <pge:LagoVistaContentPage.MainContent >
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="auto" />
        <RowDefinition Height="auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>
      <Button Command="{Binding ConnectCommand}" Text="Connect" />
      <Button Command="{Binding DisconnectCommand}" Grid.Row="1" Text="Disconnect" />
      <ListView ItemsSource="{Binding MessageTemplates}" SelectedItem="{Binding SelectedMessageTemplate}" Grid.Row="2" >
        <ListView.ItemTemplate>
          <DataTemplate>
            <ViewCell>
              <Grid HeightRequest="60">
                <Grid.RowDefinitions>
                  <RowDefinition Height="auto" />
                  <RowDefinition Height="auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="auto" />
                </Grid.ColumnDefinitions>
                <Label Text="{Binding Name}" FontSize="24" Margin="15,12,10,10" />
                <Image Source="chevron_right.png" Grid.Column="1" Margin="2,18,30,0"  WidthRequest="24" HeightRequest="24" />
                <BoxView HeightRequest="1" Color="DarkSlateGray" Grid.ColumnSpan="2" Grid.Row="1" />
              </Grid>
            </ViewCell>
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>
    </Grid>
  </pge:LagoVistaContentPage.MainContent >
</pge:LagoVistaContentPage>